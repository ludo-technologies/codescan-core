# Downloaded inputs and clones. Both are large and fully reproducible from
# fetch_data.py and clone_repos.py, so they are never committed.
data/
repos/

# Analyzer scratch output, if a run is started from this directory.
.pyscn/
__pycache__/
